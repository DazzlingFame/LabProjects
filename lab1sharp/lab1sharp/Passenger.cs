using System;
using System.Collections.Generic;
using System.Linq;
using System.Text;

namespace lab1sharp
{
    public class Passenger
    {
        public Passenger(double w)
        {
            this.we = w;
        }


       /* public List<Adult> aList = new List<Adult>();
        public List<Child> cList = new List<Child>();
        public List<Lgot> lList = new List<Lgot>();
        void info()
        {
            foreach (Adult i in aList)
                i.info();
            foreach (Child i in cList)
                i.info();
            foreach (Lgot i in lList)
                i.info();

        }*/

    }
}
